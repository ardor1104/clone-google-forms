import { palette } from './palette';

export const light = {
  headers: {
    bodyHeader: {
      bgColor: palette.white,
    },
  },
};
export const dark = {
  headers: {
    bodyHeader: {
      bgColor: '#292a2d',
    },
  },
};
