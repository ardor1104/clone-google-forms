const BREAKPOINT_UNIT = 'px';

export const breakpoints = {
  xs: `320${BREAKPOINT_UNIT}`,
  sm: `480${BREAKPOINT_UNIT}`,
  md: `768${BREAKPOINT_UNIT}`,
  lg: `992${BREAKPOINT_UNIT}`,
  xl: `1200${BREAKPOINT_UNIT}`,
};
