const FONT_UNIT = 'rem';
const RADIUS_UNIT = 'px';

export const sizes = {
  font: {
    xs: `0.625${FONT_UNIT}`,
    sm: `0.75${FONT_UNIT}`,
    md: `0.875${FONT_UNIT}`,
    lg: `1${FONT_UNIT}`,
    xl: `1.125${FONT_UNIT}`,
    xxl: `1.25${FONT_UNIT}`,
  },
  radius: {
    xs: `2${RADIUS_UNIT}`,
    sm: `4${RADIUS_UNIT}`,
    md: `6${RADIUS_UNIT}`,
    lg: `10${RADIUS_UNIT}`,
    xl: `16${RADIUS_UNIT}`,
    max: `99999${RADIUS_UNIT}`,
  },
};
